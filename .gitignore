# Go build artifacts
/bin/
*.exe
*.test
*.out
*.prof

# Local env / secrets (tokens, bot creds) — never commit
.env
*.local
/config.local.*

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
config.json

# relay-managed dynamic allowlist (may contain user ids)
allowlist.json

# runtime logs
*.log

# operator security profile + generated Claude settings
security.yaml
.claude/settings.json
